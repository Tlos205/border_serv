upstream django {
    server web:8000;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 100M;

    # Статические файлы
    location /static/ {
        alias /static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /media/;
    }

    # Всё остальное проксируем в Django
    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS-блок пока закомментирован — включишь позже, когда поставишь Let’s Encrypt
# server {
#     listen 443 ssl http2;
#     server_name  твой.домен.рф  www.твой.домен.рф;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers on;
#
#     client_max_body_size 100M;
#
#     location /static/ { alias /static/; expires 1y; add_header Cache-Control "public, immutable"; }
#     location /media/  { alias /media/; }
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }








# upstream django {
#     server web:8000;
# }

# server {
#     listen 80;
#     server_name _;
#     return 301 https://$host$request_uri;
# }

# server {
#     listen 443 ssl http2;
#     server_name example.com www.example.com;

#     # SSL (замени на свои пути или используй letsencrypt)
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;

#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers on;

#     client_max_body_size 100M;

#     location /static/ {
#         alias /static/;
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }

#     location /media/ {
#         alias /media/;
#     }

#     location / {
#         proxy_pass http://django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }